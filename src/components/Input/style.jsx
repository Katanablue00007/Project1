import { StyleSheet } from "react-native/types";

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  input:{ 
    color: '#000',
    height: 50,
    borderColor: 'Blue',
  },
 }); 